"""Thunderbird-AI: a local, always-on email -> calendar assistant.

Reads email over IMAP (read-only), uses a local Ollama model to extract
doctor appointments, bills, and vacations, writes Google Calendar events,
and notifies via Discord webhooks.
"""

__version__ = "0.1.0"
